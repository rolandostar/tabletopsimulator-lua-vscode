import * as assert from 'assert';

suite('AutoComplete Generator Tests', async () => {
  // Simple Mock test
  test('Test that the test framework works', () => {
    assert.strictEqual(1, 1);
  });
});
